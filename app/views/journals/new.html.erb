New Journal

<%= form_for([@portfolio, @position, @journal]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :entry %>
  <%= f.text_field :entry %>
  <%= f.submit %>
<% end %>

<%= link_to 'Back', portfolio_position_path(portfolio_id: @position.portfolio_id, id: @position.id) %>
